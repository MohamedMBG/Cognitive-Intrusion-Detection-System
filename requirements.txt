# Packet capture
scapy>=2.5.0

# ML
scikit-learn>=1.3.0
numpy>=1.24.0
torch>=2.0.0

# Model persistence
joblib>=1.3.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
websockets>=12.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Optional: PostgreSQL
# asyncpg>=0.29.0

# Auth (Phase 7)
PyJWT>=2.8.0
bcrypt>=4.1.0

# Optional: MLflow model registry (Phase 5)
# mlflow>=2.10.0

# Optional: Prometheus metrics (Phase 7)
# prometheus-client>=0.20.0

# Optional: OpenTelemetry tracing (Phase 7)
# opentelemetry-api>=1.22.0
# opentelemetry-sdk>=1.22.0
# opentelemetry-exporter-otlp>=1.22.0
# opentelemetry-instrumentation-fastapi>=0.43b0

# Optional: Streamlit dashboard (Phase 6)
# streamlit>=1.30.0

# Optional: GeoIP enrichment (Phase 8)
# geoip2>=4.8.0

# Optional: Webhook notifications (Phase 8)
httpx>=0.26.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Pin anyio to 3.x for pytest-asyncio compatibility
anyio>=3.7.0,<4.0.0
